#include "main.h"

/**
 * mul - takes two numbers as argument n retirs thier multiplication
 * @a : first number
 * @b : second number to be multiplied
 *
 * Return: returns the result of the two numbers multiplied
 *
 */
int mul(int a, int b)
{
	return (a * b);
}
