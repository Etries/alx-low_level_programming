#include <stdio.h>

/**
*main - display all digits between 0 and 9
*
*Return: will return (0) (success)
*
*/
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	putchar('\n');
	return (0);
}
